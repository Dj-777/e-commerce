export enum Sort {
  HighToLow = 'Price--HighToLow',
  LowToHigh = 'Price--LowToHigh',
  NewestFirst = 'NewestFirst',
}

// export enum Filter {
//   Category = 'Electronics-gadegets' || 'Sport' || 'Jwellery' || 'clothes',
// }

export enum Category {
  Electronics = 'Electronics',
  Sport = 'Sport',
}
export enum Brand {
  Electronics = 'sansung' || 'vivo',
}
